# The Reader's Ledger

A 30-day RPG-style reading tracker. Log pages, earn XP, level up, and spend your progress on real rewards.

Built as a single-file HTML app with localStorage persistence.

## Deploy

Deployed via Vercel. Any push to `main` triggers an automatic redeploy.
